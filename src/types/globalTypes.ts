export interface MatchParams {
  id: string;
}

export interface IUserData {
  loginEmail: string,
  loginName: string,
  loginPassword: string,
  regEmail: string,
  regName: string,
  regPass: string,
  regRepeatPass: string
}
